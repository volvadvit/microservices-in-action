spring:
  application:
    name: licensing-service
#  profiles:
#    active: dev
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: http://config-server:8888

server:
  port: 8081